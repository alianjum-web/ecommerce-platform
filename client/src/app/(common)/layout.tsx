/**
 * Shared routes used across roles: auth, order tracking, notifications.
 * URLs are unchanged (route groups do not affect paths).
 */
export default function CommonRoutesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
